---
type: runbook
workstream: field_ops
capability: job materials tracking (per-job Materials page, migration 0059)
audience: Successor-Operator
status: skeleton
---

# Runbook — per-job Materials tracking

<!-- BEGIN TREE-XREF (generated by scripts/build_runbook_xrefs.py — do not edit inside) -->
> **In the troubleshooting tree** ([printable guide](../troubleshooting/troubleshooting_guide.md) · dashboard `/troubleshoot`). This runbook resolves:
>   - Field-ops sync — portal job/hours/materials/equipment/incidents to Smartsheet → A job / hours / materials record is created or edited in the portal → “A manager cannot mark a delivery on a job's Materials page, or a line shows the wrong received total, or a line still shows a problem after the goods arrived.”
<!-- END TREE-XREF -->

> §43 successor-remediation skeleton. Companion to `material_catalog_admin.md` (the material
> TYPE vocabulary) — this covers the per-job **expected materials**, their scheduled loads, and
> the delivery ledger. The polished §6a operator guide lands with the enablement-doc program.

## What it is

The **Materials tracking** page (`/jobs/<job id>/materials`) is where a job's expected materials
live. It is reached by deep link only — there is no home card, because the page is job-scoped:

- from the **Job Tracker** → a job's detail → *Materials tracking →*
- from **My Tasks → Daily report** → the *Expected materials* region → *Materials tracking →*

It is a portal surface: D1 only, no send, no AI. Everything is capability-gated server-side; the
buttons the page shows are affordances, not the boundary.

| Who | What they can do |
|---|---|
| **Admin** (`cap.materials.manage`) | Author the expected list — description or catalog type, part number, category, qty/unit, expected **ship** and **delivery** dates — plus the scheduled **loads** under each line. Also everything a manager can do. |
| **Manager** (`cap.materials.receive` + a manager/admin role) | Mark each line **Delivered / Partially delivered / Not delivered**, each with a note, an optional quantity and an optional load reference. |
| **Field PM / submitter** | Read the list for the job they are placed on. No marking, no editing. |

### The one idea worth understanding: marks are a LEDGER, not a flag

A part number routinely arrives across several truckloads. So a delivery mark **appends an event**
(`material_receipt_events`) rather than flipping a switch, and the line's displayed state is a
**rollup** over those events:

- the pill shows the **latest** mark; the running total is the **sum** of every event's quantity
- **marking the same line again is normal and expected** — partial, partial, then delivered
- an event **cannot be deleted** — the ledger is append-only by design. A mistake is corrected by
  recording a further event, not by erasing one.

Two states sit on **different axes** and must not be confused:

- the **delivery rollup** (not marked / partial / delivered / not delivered) — from the ledger
- the **problem flag** (`incident`) — set by *Report a problem* on the daily form. It is **sticky**:
  recording a later delivery against a flagged line **records the delivery but does not clear the
  flag**, and every surface (page, daily form, the Material List sheet) keeps showing the problem.
  That is deliberate — a delivered-but-damaged pallet is still a problem.

## Symptoms

| Symptom | What it means | What the Successor-Operator does |
|---|---|---|
| A manager has no **Delivered / Partially delivered / Not delivered** buttons | Marking needs `cap.materials.receive` **and** a manager/admin role. A submitter holds the capability but not the role — by design, the same gate the daily report uses | Check the account's role in **Accounts**. If they should be marking deliveries, they need the manager role — not a new capability |
| "You're not assigned to that job" (403 `forbidden_job`) | Non-admins only reach the job they are **placed on** (`personnel.current_job`) | Place them on the job via **Personnel** / the job's crew. This is the designed scope, not a fault |
| The *Materials tracking →* button is missing from the job page or the daily report | The account lacks `cap.materials.receive` | Confirm the role in **Accounts**. All three roles normally hold it |
| The page says "Nothing is on this job's materials list yet" | No expected lines have been authored for the job | Not a fault. The office (admin) adds them on this page — or, once manifest import ships, loads them from the BOM |
| "Could not load this job's materials" | Transient read failure — the page says so and offers **Retry** rather than showing an empty list | Tap **Retry**. If persistent, check whether the job's detail page loads at all, then escalate |
| A line shows the **wrong running total** | The total is the SUM of every event's quantity. A mark recorded without a quantity contributes nothing to it; a duplicate mark contributes twice | Open the line's history — every event shows its quantity, note, date and who recorded it. If a genuine duplicate is there, record a correcting event; the ledger cannot be edited in place. Escalate if the total must be corrected at source |
| A line still shows **Problem reported** after the goods arrived | Working as designed — the incident flag is sticky and a delivery mark does not clear it | Not a fault. The delivery IS recorded (check the history). Clearing a flag is not a Tier-2 action — escalate if the flag is genuinely wrong |
| "Add a note explaining the shortfall" when marking **Not delivered** | A non-delivery must say why — the note is required, and a quantity is refused on it | Add the note. If a quantity genuinely arrived, the mark is *Partially delivered*, not *Not delivered* |
| "That load isn't on this material line" | The chosen load belongs to a different line | Pick one of that line's own loads, or leave the load blank — the mark records fine without one |
| A load can't be removed / a line can't be edited | Line editing is admin-only, and a line that has been received or flagged is a **record** — content edits are locked (`not_editable`) | Expected. If the record itself is wrong, escalate |
| "Confirm receipt" on the daily form returns **already received** (409) | Pre-0059 behaviour that should no longer occur — repeats are now legal | The deployed Worker predates migration `0059`. Check the deploy; escalate if the deployed code is current |

## Escalate to Seth (Developer-Operator) — high-capability-class

- **Correcting or removing a ledger event.** Append-only is structural; there is no delete path
  and one must not be improvised.
- **Clearing a stuck `incident` flag** on a line.
- Anything touching the **§51 Material List Smartsheet mirror**, which is one-way-up permanently.
- **Data loss / restore.** `material_receipt_events` and `material_shipments` are **D1-primary**.
  A mark made on the daily form also lands in that day's filed PDF ("Deliveries Received") on Box,
  and the coarse per-line state mirrors up to the Material List sheet when
  `field_ops.fieldops_sync.materials_enabled` is on — but the **per-event history of a mark made on
  this page** exists only in D1. The restore path is Cloudflare **D1 Time Travel** (30-day
  point-in-time restore of the whole database). See `docs/tech_debt.md` → "D1-primary tables have
  no ITS-side backup", whose revisit trigger this capability fired.
