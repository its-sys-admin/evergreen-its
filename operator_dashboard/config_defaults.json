{
  "schema_version": 1,
  "description": "ITS_Config data dictionary — generated by scripts/generate_config_dictionary.py from the in-repo daemon REQUIRED_CONFIG declarations. Network-free, deterministic.",
  "keys": [
    {
      "setting": "field_ops.box.archive_root_folder_id",
      "workstream": "field_ops",
      "kind": "str",
      "default": "",
      "purpose": "Box root the Track 6 job archive relocates a closed job's Safety and Progress containers beneath (ITS Archive/<Job>/<Workstream>). Built by build_box_roots.py.",
      "read_by": [
        "field_ops.job_archive"
      ],
      "origin": "daemon"
    },
    {
      "setting": "field_ops.fieldops_sync.archive_enabled",
      "workstream": "field_ops",
      "kind": "bool",
      "default": false,
      "purpose": "Gate for the Track 6 job-archive pass: drains the portal's archive queue and relocates a closed job's six Smartsheet + Box containers into the archive (and back). Ships OFF; until it is on, the portal's Archive button records intent that nothing acts on.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "field_ops.fieldops_sync.equipment_enabled",
      "workstream": "field_ops",
      "kind": "bool",
      "default": false,
      "purpose": "Per-stream gate: mirror equipment status from the portal into Smartsheet.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "field_ops.fieldops_sync.hours_enabled",
      "workstream": "field_ops",
      "kind": "bool",
      "default": false,
      "purpose": "Per-stream gate: mirror crew hours from the portal into Smartsheet.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "field_ops.fieldops_sync.incidents_enabled",
      "workstream": "field_ops",
      "kind": "bool",
      "default": false,
      "purpose": "Per-stream gate: mirror material incidents from the portal into Smartsheet.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "field_ops.fieldops_sync.materials_enabled",
      "workstream": "field_ops",
      "kind": "bool",
      "default": false,
      "purpose": "Per-stream gate: mirror material receipts from the portal into Smartsheet. (Activation is gated on the §51 rider — read the row's Description before flipping.)",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "field_ops.fieldops_sync.sync_enabled",
      "workstream": "field_ops",
      "kind": "bool",
      "default": false,
      "purpose": "Master gate for the portal→Smartsheet job mirror (fieldops_sync). Ships OFF; the operator flips it on at cutover after the mirror slices land.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "field_ops.manifest_poll.polling_enabled",
      "workstream": "field_ops",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the field_ops.manifest_poll daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "field_ops.manifest_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "alerting.dedupe_window_minutes",
      "workstream": "global",
      "kind": "int",
      "default": 60,
      "purpose": "How long (minutes) a repeated CRITICAL alert is suppressed on the push legs (email + Sentry) before it can fire again. The per-occurrence ITS_Errors record is never suppressed.",
      "read_by": [
        "shared.alert_dedupe"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "alerting.max_alerts_per_hour",
      "workstream": "global",
      "kind": "int",
      "default": 15,
      "purpose": "Global ceiling on operator alert emails per hour across all keys, so a flapping failure cannot fire unbounded email. The record is never capped — only the email fan-out.",
      "read_by": [
        "shared.alert_dedupe"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "circuit_breaker.cooldown_seconds",
      "workstream": "global",
      "kind": "int",
      "default": 300,
      "purpose": "How long (seconds) the breaker stays open before a trial half-open call.",
      "read_by": [
        "shared.circuit_breaker"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "circuit_breaker.enabled",
      "workstream": "global",
      "kind": "bool",
      "default": true,
      "purpose": "Whether the Smartsheet circuit breaker is armed. When tripped it short-circuits Smartsheet calls during an outage to fail fast.",
      "read_by": [
        "shared.circuit_breaker"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "circuit_breaker.failure_threshold",
      "workstream": "global",
      "kind": "int",
      "default": 5,
      "purpose": "Consecutive Smartsheet failures before the breaker opens (trips).",
      "read_by": [
        "shared.circuit_breaker"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "circuit_breaker.prolonged_open_alert_seconds",
      "workstream": "global",
      "kind": "int",
      "default": 600,
      "purpose": "How long (seconds) the breaker may stay open before the watchdog fires a prolonged-open CRITICAL page.",
      "read_by": [
        "watchdog"
      ],
      "origin": "daemon"
    },
    {
      "setting": "picklist_sync.size_hard_halt_threshold",
      "workstream": "global",
      "kind": "int",
      "default": 400,
      "purpose": "Option count that HARD-HALTS that one mapping's sync (a runaway guardrail).",
      "read_by": [
        "run_picklist_sync",
        "shared.picklist_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "picklist_sync.size_warn_threshold",
      "workstream": "global",
      "kind": "int",
      "default": 200,
      "purpose": "Option count on a synced picklist that triggers a WARN (a large but still-processed list).",
      "read_by": [
        "run_picklist_sync",
        "shared.picklist_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "smartsheet.retry.backoff_seconds",
      "workstream": "global",
      "kind": "str",
      "default": "2.0,5.0",
      "purpose": "Comma-separated wait (seconds) before each extra attempt, e.g. '2.0,5.0'. The last value repeats if there are more attempts than entries.",
      "read_by": [
        "shared.smartsheet_client"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "smartsheet.retry.enabled",
      "workstream": "global",
      "kind": "bool",
      "default": true,
      "purpose": "Whether ITS re-issues a Smartsheet READ that failed with a 5xx or a network timeout — the two classes the Smartsheet SDK does not retry itself. Writes are never retried. Set false for a pure pass-through escape hatch.",
      "read_by": [
        "shared.smartsheet_client"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "smartsheet.retry.max_extra_attempts",
      "workstream": "global",
      "kind": "int",
      "default": 2,
      "purpose": "How many EXTRA attempts a failed Smartsheet read gets before the error is raised to the caller (0 = no retry).",
      "read_by": [
        "shared.smartsheet_client"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "smartsheet.sheet_count_ceiling",
      "workstream": "global",
      "kind": "int",
      "default": 1500,
      "purpose": "Per-workspace sheet-count ceiling; a new week/period sheet that would land past it routes to the Review Queue instead of being created silently.",
      "read_by": [
        "shared.sheet_capacity"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "smartsheet.sheet_count_margin",
      "workstream": "global",
      "kind": "int",
      "default": 50,
      "purpose": "Headroom below the ceiling at which the sheet-capacity guard starts warning.",
      "read_by": [
        "shared.sheet_capacity"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "system.heartbeat_url",
      "workstream": "global",
      "kind": "str",
      "default": "",
      "purpose": "The external UptimeRobot heartbeat URL the watchdog pings each run so a total MacBook-death (the watchdog can't alert about itself) is caught.",
      "read_by": [
        "watchdog"
      ],
      "origin": "daemon"
    },
    {
      "setting": "system.operator_email",
      "workstream": "global",
      "kind": "str",
      "default": "seth@solutionsmith.org",
      "purpose": "Where out-of-band operator alerts (Resend) are sent when ITS_Config cannot be read — the last-resort page recipient during a Smartsheet outage.",
      "read_by": [
        "shared.resend_client"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "system.state",
      "workstream": "global",
      "kind": "str",
      "default": "ACTIVE",
      "purpose": "The system kill switch. ACTIVE = normal; PAUSED / MAINTENANCE make every daemon exit cleanly at entry. Fail-open: an unreadable value is treated as ACTIVE. This is an operator-convenience pause, not a security control.",
      "read_by": [
        "shared.kill_switch"
      ],
      "origin": "shared-infra"
    },
    {
      "setting": "po_materials.config_actuator.polling_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime gate for the §50 config actuator daemon (applies approved workstream-config changes on the Mac).",
      "read_by": [
        "po_materials.config_actuator"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.confidence_threshold",
      "workstream": "po_materials",
      "kind": "float",
      "default": 0.75,
      "purpose": "Minimum extraction confidence to post 'extracted'; below it the doc degrades to needs_review (the disposition screen's manual Tier-3).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.model",
      "workstream": "po_materials",
      "kind": "str",
      "default": "qwen3.5:9b",
      "purpose": "Pinned local Ollama model for Tier-2 extraction; swapping it re-runs the offline corpus eval to re-qualify (ADR-0004 decision 1).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.ocr_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Gate for the macOS-Vision OCR pass (estimate_ocr) feeding Tier-2 on SCANNED documents. Ships FALSE (dark).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.ollama_base_url",
      "workstream": "po_materials",
      "kind": "str",
      "default": "http://127.0.0.1:11434",
      "purpose": "Local Ollama base URL for Tier-2 extraction (localhost-only — vendor pricing never leaves the machine).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.tier1_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Gate for the Tier-1 deterministic native-text extraction (estimate_parse template→generic ladder). Ships FALSE (dark).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.tier1_xlsx_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Gate for the Tier-1 deterministic VENDOR-SPREADSHEET extraction (estimate_parse.parse_xlsx_estimate: sandboxed openpyxl grid -> the same generic-table line logic as the PDF tier, with doc-level totals read from the CELL grid). NO AI. Separate from tier1_enabled because it is a distinct parse path with its own failure modes. Turning it ON lets a vendor .xlsx quote be extracted instead of hand-keyed; turning it OFF returns those documents to needs_review — no other lane is affected. Independent of Tier 0, which claims ITS's own HMAC-verified quote form first.",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.tier2_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Gate for the Tier-2 LOCAL-Ollama schema-constrained extraction (estimate_extract; at most one Tier-2 doc per cycle). Ships FALSE (dark).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_extract.timeout_seconds",
      "workstream": "po_materials",
      "kind": "int",
      "default": 600,
      "purpose": "Wall-clock budget in seconds for one Tier-2 extraction call (keep_alive=0 load-on-demand can make the first call slow).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_poll.max_pages_preview",
      "workstream": "po_materials",
      "kind": "int",
      "default": 12,
      "purpose": "Max pages rendered as disposition-screen previews per estimate (Quartz via the estimate_sandbox child).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.estimate_poll.polling_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the po_materials.estimate_poll daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "po_materials.estimate_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.po_attach_screen.clamav_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Shared §34 ClamAV posture; owned by po_materials. One scanner setting spans every document pool (PO attachments, estimates, manifests).",
      "read_by": [
        "field_ops.manifest_poll",
        "po_materials.estimate_poll",
        "po_materials.po_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.po_poll.polling_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime gate for the PO pull daemon (pulls submitted POs from the Worker). Ships dark.",
      "read_by": [
        "po_materials.po_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.po_poll.status_sync_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Sub-gate: sync PO statuses back to the portal. Ships dark.",
      "read_by": [
        "po_materials.po_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.po_poll.vendors_sync_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Sub-gate: push the vendor list down to the portal PO dropdown. Ships dark.",
      "read_by": [
        "po_materials.po_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.po_send.from_mailbox",
      "workstream": "po_materials",
      "kind": "str",
      "default": "procurement@evergreenmirror.com",
      "purpose": "The M365 mailbox the po_materials.po_send send daemon sends approved email FROM.",
      "read_by": [
        "po_materials.po_send",
        "po_materials.po_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.po_send.polling_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the po_materials.po_send daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "po_materials.po_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.po_send.scheduled_send_local",
      "workstream": "po_materials",
      "kind": "str",
      "default": "MON 07:00",
      "purpose": "Local-time window (e.g. `MON 07:00`) at/after which a row approved with **Approve for Scheduled Send** may dispatch on the po_materials.po_send path.",
      "read_by": [
        "po_materials.po_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.rfq_poll.polling_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the po_materials.rfq_poll daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "po_materials.rfq_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.rfq_send.from_mailbox",
      "workstream": "po_materials",
      "kind": "str",
      "default": "procurement@evergreenmirror.com",
      "purpose": "The M365 mailbox the po_materials.rfq_send send daemon sends approved email FROM.",
      "read_by": [
        "po_materials.rfq_send",
        "po_materials.rfq_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.rfq_send.polling_enabled",
      "workstream": "po_materials",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the po_materials.rfq_send daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "po_materials.rfq_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "po_materials.rfq_send.scheduled_send_local",
      "workstream": "po_materials",
      "kind": "str",
      "default": "MON 07:00",
      "purpose": "Local-time window (e.g. `MON 07:00`) at/after which a row approved with **Approve for Scheduled Send** may dispatch on the po_materials.rfq_send path.",
      "read_by": [
        "po_materials.rfq_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.portal.worker_base_url",
      "workstream": "po_materials",
      "kind": "str",
      "default": "",
      "purpose": "Base URL of the Safety Portal Cloudflare Worker. The portal pull / PO / progress daemons hit its send-free internal API here. Repointed to the custom domain (safety.evergreenmirror.com) after deploy.",
      "read_by": [
        "po_materials.config_actuator"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.box.portal_root_folder_id",
      "workstream": "progress_reports",
      "kind": "str",
      "default": "",
      "purpose": "Box root folder ID under which progress-report packets are filed.",
      "read_by": [
        "field_ops.job_archive",
        "progress_reports.progress_weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.compile_now_poll.polling_enabled",
      "workstream": "progress_reports",
      "kind": "bool",
      "default": true,
      "purpose": "Runtime on/off gate for the progress_reports.compile_now_poll daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "safety_reports.compile_now_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.equipment_status.row_cap_warn_threshold",
      "workstream": "progress_reports",
      "kind": "int",
      "default": 15000,
      "purpose": "Row-count on the mirror sheet at which progress_reports.equipment_status WARNs that the sheet is approaching the Smartsheet per-sheet row cap.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.evergreen_contact_name",
      "workstream": "progress_reports",
      "kind": "str",
      "default": "the Evergreen Renewables office",
      "purpose": "The name ITS uses for the Evergreen Renewables office/contact in this workstream's report copy.",
      "read_by": [
        "progress_reports.progress_weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.hours_log.row_cap_warn_threshold",
      "workstream": "progress_reports",
      "kind": "int",
      "default": 15000,
      "purpose": "Row-count on the mirror sheet at which progress_reports.hours_log WARNs that the sheet is approaching the Smartsheet per-sheet row cap.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.material_incidents.row_cap_warn_threshold",
      "workstream": "progress_reports",
      "kind": "int",
      "default": 15000,
      "purpose": "Row-count on the mirror sheet at which progress_reports.material_incidents WARNs that the sheet is approaching the Smartsheet per-sheet row cap.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.material_list.row_cap_warn_threshold",
      "workstream": "progress_reports",
      "kind": "int",
      "default": 15000,
      "purpose": "Row-count on the mirror sheet at which progress_reports.material_list WARNs that the sheet is approaching the Smartsheet per-sheet row cap.",
      "read_by": [
        "field_ops.fieldops_sync"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.progress_send.from_mailbox",
      "workstream": "progress_reports",
      "kind": "str",
      "default": "progress@evergreenmirror.com",
      "purpose": "The M365 mailbox the progress_reports.progress_send send daemon sends approved email FROM.",
      "read_by": [
        "progress_reports.progress_send",
        "progress_reports.progress_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.progress_send.polling_enabled",
      "workstream": "progress_reports",
      "kind": "bool",
      "default": true,
      "purpose": "Runtime on/off gate for the progress_reports.progress_send daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "progress_reports.progress_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.progress_send.scheduled_send_local",
      "workstream": "progress_reports",
      "kind": "str",
      "default": "MON 07:00",
      "purpose": "Local-time window (e.g. `MON 07:00`) at/after which a row approved with **Approve for Scheduled Send** may dispatch on the progress_reports.progress_send path.",
      "read_by": [
        "progress_reports.progress_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.progress_weekly_generate.job_timeout_seconds",
      "workstream": "progress_reports",
      "kind": "int",
      "default": 600,
      "purpose": "Per-job wall-clock ceiling (seconds) for the progress_reports.progress_weekly_generate weekly compile; a job exceeding it is fenced to the Review Queue, not left to hang.",
      "read_by": [
        "progress_reports.progress_weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.progress_weekly_generate.merge_memory_ceiling_bytes",
      "workstream": "progress_reports",
      "kind": "int",
      "default": 268435456,
      "purpose": "Memory ceiling (bytes) for the progress_reports.progress_weekly_generate PDF-merge step; a packet whose merge would exceed it is refused rather than risk OOMing the daemon.",
      "read_by": [
        "progress_reports.progress_weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.portal.worker_base_url",
      "workstream": "progress_reports",
      "kind": "str",
      "default": "",
      "purpose": "Shared Worker base URL, read under progress_reports for the P6 rollup page.",
      "read_by": [
        "progress_reports.progress_weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "progress_reports.intake_enabled",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": false,
      "purpose": "FOOTGUN: the progress-intake gate is read under Workstream='safety_reports' (intake's own workstream), NOT 'progress_reports' — seed it there.",
      "read_by": [
        "safety_reports.intake"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.box.portal_root_folder_id",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "",
      "purpose": "Shared Box mirror-tree root; owned by safety_reports. Unset means the filing folder cannot resolve and rows stay claimed until it is configured.",
      "read_by": [
        "field_ops.job_archive",
        "field_ops.manifest_poll",
        "po_materials.estimate_poll",
        "po_materials.po_poll",
        "po_materials.rfq_poll",
        "safety_reports.intake",
        "safety_reports.portal_poll",
        "safety_reports.weekly_generate",
        "subcontracts.subcontract_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.compile_now_poll.polling_enabled",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": true,
      "purpose": "Runtime on/off gate for the safety_reports.compile_now_poll daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "safety_reports.compile_now_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.evergreen_contact_name",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "the Evergreen Renewables office",
      "purpose": "The name ITS uses for the Evergreen Renewables office/contact in this workstream's report copy.",
      "read_by": [
        "safety_reports.weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.intake.allowed_senders",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "",
      "purpose": "Comma-separated sender allowlist for the intake extraction path (the retired email-PDF intake; the live path is the portal PULL). Empty = none set.",
      "read_by": [
        "safety_reports.intake"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.intake.box_filing_enabled",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": true,
      "purpose": "Whether intake files the rendered safety PDF to Box. Off keeps the pipeline running but skips the Box upload.",
      "read_by": [
        "safety_reports.intake"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.intake.classification_model",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "claude-sonnet-4-6",
      "purpose": "The Anthropic model the intake extraction/classification step uses. Legacy email-intake path; dormant.",
      "read_by": [
        "safety_reports.intake"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.intake.confidence_threshold",
      "workstream": "safety_reports",
      "kind": "float",
      "default": 0.75,
      "purpose": "Extraction-confidence floor (0–1). Below it, an item routes to the Review Queue instead of being trusted (Op Stds confidence scoring).",
      "read_by": [
        "safety_reports.intake"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.intake.mailbox",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "safety@evergreenmirror.com",
      "purpose": "The mailbox the (now-dormant, legacy) safety email-intake path read from. The live path is the portal PULL model; this remains for the retired email caller.",
      "read_by": [
        "safety_reports.intake"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.intake.review_queue_on_low_confidence",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": true,
      "purpose": "Whether a below-threshold extraction is routed to the Review Queue (true) rather than dropped.",
      "read_by": [
        "safety_reports.intake"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.photo_screen.clamav_enabled",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": false,
      "purpose": "Turns on the ClamAV leg of the §34 photo screen (magic + Pillow verify + re-encode always run; this adds the AV scan). Default OFF.",
      "read_by": [
        "safety_reports.intake",
        "safety_reports.portal_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.portal.worker_base_url",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "",
      "purpose": "Shared Worker base URL; owned by safety_reports, read here too.",
      "read_by": [
        "field_ops.fieldops_sync",
        "field_ops.manifest_poll",
        "po_materials.estimate_poll",
        "po_materials.po_poll",
        "po_materials.rfq_poll",
        "safety_reports.portal_poll",
        "safety_reports.publish_daemon",
        "subcontracts.subcontract_poll",
        "watchdog"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.portal_poll.polling_enabled",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": true,
      "purpose": "Runtime on/off gate for the safety_reports.portal_poll daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "safety_reports.portal_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.publish_daemon.polling_enabled",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the safety_reports.publish_daemon daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "safety_reports.publish_daemon"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.weekly_generate.job_timeout_seconds",
      "workstream": "safety_reports",
      "kind": "int",
      "default": 600,
      "purpose": "Per-job wall-clock ceiling (seconds) for the safety_reports.weekly_generate weekly compile; a job exceeding it is fenced to the Review Queue, not left to hang.",
      "read_by": [
        "safety_reports.weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.weekly_generate.merge_memory_ceiling_bytes",
      "workstream": "safety_reports",
      "kind": "int",
      "default": 268435456,
      "purpose": "Memory ceiling (bytes) for the safety_reports.weekly_generate PDF-merge step; a packet whose merge would exceed it is refused rather than risk OOMing the daemon.",
      "read_by": [
        "safety_reports.weekly_generate"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.weekly_send.from_mailbox",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "safety@evergreenmirror.com",
      "purpose": "The M365 mailbox the safety_reports.weekly_send send daemon sends approved email FROM.",
      "read_by": [
        "safety_reports.weekly_send",
        "safety_reports.weekly_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.weekly_send.polling_enabled",
      "workstream": "safety_reports",
      "kind": "bool",
      "default": true,
      "purpose": "Runtime on/off gate for the safety_reports.weekly_send daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "safety_reports.weekly_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "safety_reports.weekly_send.scheduled_send_local",
      "workstream": "safety_reports",
      "kind": "str",
      "default": "MON 07:00",
      "purpose": "Local-time window (e.g. `MON 07:00`) at/after which a row approved with **Approve for Scheduled Send** may dispatch on the safety_reports.weekly_send path.",
      "read_by": [
        "safety_reports.weekly_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "subcontracts.subcontract_poll.polling_enabled",
      "workstream": "subcontracts",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the subcontracts.subcontract_poll daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "subcontracts.subcontract_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "subcontracts.subcontract_poll.status_sync_enabled",
      "workstream": "subcontracts",
      "kind": "bool",
      "default": false,
      "purpose": "Sub-gate for subcontracts.subcontract_poll: sync statuses back to the portal.",
      "read_by": [
        "subcontracts.subcontract_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "subcontracts.subcontract_poll.subcontractors_sync_enabled",
      "workstream": "subcontracts",
      "kind": "bool",
      "default": false,
      "purpose": "Gate for subcontract_poll's §51 subcontractor-sync passes: the ITS_Subcontractors full-replace down-sync into the Worker's D1 cache AND the dirty-subcontractor up-sync back into ITS_Subcontractors (bridge-key find-or-create by Sub Key). Ships dark.",
      "read_by": [
        "subcontracts.subcontract_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "subcontracts.subcontract_send.from_mailbox",
      "workstream": "subcontracts",
      "kind": "str",
      "default": "procurement@evergreenmirror.com",
      "purpose": "The M365 mailbox the subcontracts.subcontract_send send daemon sends approved email FROM.",
      "read_by": [
        "subcontracts.subcontract_send",
        "subcontracts.subcontract_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "subcontracts.subcontract_send.polling_enabled",
      "workstream": "subcontracts",
      "kind": "bool",
      "default": false,
      "purpose": "Runtime on/off gate for the subcontracts.subcontract_send daemon. False pauses it without unloading its launchd job (the canonical runtime gate, distinct from the report-filter Enabled checkbox).",
      "read_by": [
        "subcontracts.subcontract_send_poll"
      ],
      "origin": "daemon"
    },
    {
      "setting": "subcontracts.subcontract_send.scheduled_send_local",
      "workstream": "subcontracts",
      "kind": "str",
      "default": "MON 07:00",
      "purpose": "Local-time window (e.g. `MON 07:00`) at/after which a row approved with **Approve for Scheduled Send** may dispatch on the subcontracts.subcontract_send path.",
      "read_by": [
        "subcontracts.subcontract_send_poll"
      ],
      "origin": "daemon"
    }
  ]
}
